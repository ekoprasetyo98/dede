<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dedee">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".AdminActivity"></activity>
        <activity android:name=".AlergiActivity" />
        <activity android:name=".HasilDiagnosa" />
        <activity android:name=".NeuroActivity" />
        <activity android:name=".KontakActivity" />
        <activity android:name=".AtopicActivity" />
        <activity android:name=".EducationActivity" />
        <activity android:name=".DiagnosaActivity" />
        <activity android:name=".menuUtamaActivity" />
        <activity android:name=".registrasiActivity" />
        <activity android:name=".profilActivity" />
        <activity android:name=".splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".dashboardActivity" />
    </application>

</manifest>